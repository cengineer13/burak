{
  "name": "burak",
  "version": "1.0.0",
  "description": "This is burak restaurant express JS backend project",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "start:dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "train": "nodemon --exec ts-node  src/train.ts",
    "python": "nodemon --exec python train.py",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marcus (Murodil)",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.14",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "moment": "^2.31.0",
    "ts-node": "^10.9.1"
  }
}